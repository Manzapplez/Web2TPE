<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SoundSnack</title>

    <link rel="stylesheet" href="app/css/homeStyle.css">
    <link rel="stylesheet" href="app/css/formStyle.css">
    <link rel="stylesheet" href="app/css/adminStyle.css">
</head>

<body>
    <header>
        <div class="header-container">
            <div class="site-title">
                <h1><a href="home">SoundSnack</a></h1>
                <p>Tu música, a un clic</p>
            </div>

            <div class="user-info">
                <?php if ($user): ?>
                    <!-- Si la sesión está activa -->
                    <div class="user-details">
                        <p class="username">
                            <?= isset($user['name']) ? htmlspecialchars($user['name']) : 'Usuario' ?>
                        </p>
                        <img class="profile-pic"
                            src="<?= isset($user['profilePhoto']) && !empty($user['profilePhoto'])
                                        ? htmlspecialchars($user['profilePhoto'])
                                        : 'assets/img/covers/users/default.jpg' ?>"
                            alt="Foto de perfil de <?= htmlspecialchars($user['name'] ?? 'usuario') ?>" />
                        <p class="email"><?= isset($user['email']) ? htmlspecialchars($user['email']) : '' ?></p>

                        <div class="logout-button">
                            <a class="btn btn-logout" href="logOut">Log Out</a>
                        </div>
                    </div>

                <?php else: ?>
                    <!-- Si la sesión no está activa -->
                    <div class="user-details">
                        <p class="username">Guest4821</p>
                        <img class="profile-pic" src="assets/img/covers/users/default.jpg" alt="Foto de perfil genérica" />
                        <p class="email">guest4821@example.com</p>

                        <div class="auth-buttons">
                            <div class="login-button">
                                <a class="btn btn-login" href="login">Sign In</a>
                            </div>
                            <div class="register-button">
                                <a class="btn btn-register" href="register">Sign Up</a>
                            </div>
                        </div>
                    </div>
                <?php endif; ?>
            </div>
        </div>
    </header>